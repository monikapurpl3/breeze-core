[pytest]
testpaths = tests
# The suite mixes plain and async tests; auto mode means an async test doesn't
# need an explicit @pytest.mark.asyncio to run (and, more importantly, doesn't
# silently fail if someone forgets one).
asyncio_mode = auto
